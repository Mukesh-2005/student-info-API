# 🏗️ Project Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         STUDENT INFO API                             │
└─────────────────────────────────────────────────────────────────────┘

                              USER/CLIENT
                                   │
                                   │ HTTP Requests
                                   ▼
┌────────────────────────────────────────────────────────────────────┐
│                          main.py (FastAPI)                          │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  Endpoints:                                                 │   │
│  │  • POST   /students/          → create_student()           │   │
│  │  • GET    /students/          → get_all_students()         │   │
│  │  • GET    /students/{id}      → get_student()              │   │
│  │  • PUT    /students/{id}      → update_student()           │   │
│  │  • DELETE /students/{id}      → delete_student()           │   │
│  │  • GET    /students/search/   → search_students()          │   │
│  └────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘
                                   │
                                   │ Uses
                                   ▼
┌────────────────────────────────────────────────────────────────────┐
│                      schemas.py (Pydantic)                          │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  Data Validation Models:                                    │   │
│  │  • StudentCreate   → Validates incoming data                │   │
│  │  • StudentUpdate   → Validates update data                  │   │
│  │  • StudentResponse → Formats outgoing data                  │   │
│  │                                                              │   │
│  │  Validates: email format, age range, GPA range, etc.        │   │
│  └────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘
                                   │
                                   │ Uses
                                   ▼
┌────────────────────────────────────────────────────────────────────┐
│                    database.py (SQLAlchemy Setup)                   │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  • Creates database engine                                  │   │
│  │  • Manages database sessions                                │   │
│  │  • Provides get_db() dependency                             │   │
│  └────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘
                                   │
                                   │ Connects to
                                   ▼
┌────────────────────────────────────────────────────────────────────┐
│                     models.py (ORM Models)                          │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  Student Model (Database Table):                            │   │
│  │  ┌──────────────────────────────────────────────────┐      │   │
│  │  │  Column       │  Type    │  Constraints          │      │   │
│  │  ├──────────────────────────────────────────────────┤      │   │
│  │  │  id           │  Integer │  Primary Key          │      │   │
│  │  │  name         │  String  │  Not Null             │      │   │
│  │  │  age          │  Integer │  Not Null             │      │   │
│  │  │  grade        │  String  │  Not Null             │      │   │
│  │  │  email        │  String  │  Unique, Not Null     │      │   │
│  │  │  gpa          │  Float   │  Nullable             │      │   │
│  │  └──────────────────────────────────────────────────┘      │   │
│  └────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘
                                   │
                                   │ Stored in
                                   ▼
                        ┌─────────────────────┐
                        │   students.db       │
                        │   (SQLite File)     │
                        └─────────────────────┘
```

## 🔄 Request Flow Example

### Creating a Student (POST /students/)

```
1. USER sends POST request with JSON:
   {
     "name": "Alice",
     "age": 20,
     "grade": "A",
     "email": "alice@test.com",
     "gpa": 3.8
   }
   
2. FastAPI (main.py) receives request
   ↓
3. Pydantic (schemas.py) validates data
   - Is age between 5-100? ✓
   - Is email format valid? ✓
   - Is GPA between 0.0-4.0? ✓
   ↓
4. Function create_student() executes
   - Checks if email exists
   - Creates Student model instance
   ↓
5. SQLAlchemy (models.py) converts to SQL
   INSERT INTO students (name, age, grade, email, gpa)
   VALUES ('Alice', 20, 'A', 'alice@test.com', 3.8);
   ↓
6. Database (students.db) stores data
   ↓
7. Response sent back to user:
   {
     "id": 1,
     "name": "Alice",
     "age": 20,
     "grade": "A",
     "email": "alice@test.com",
     "gpa": 3.8
   }
```

## 📦 File Dependencies

```
main.py
  ├── imports models.py (Student model)
  ├── imports schemas.py (validation models)
  └── imports database.py (get_db dependency)

models.py
  └── imports database.py (Base class)

schemas.py
  └── standalone (only uses Pydantic)

database.py
  └── standalone (only uses SQLAlchemy)
```

## 🎯 Key Concepts Visual

### 1. ORM (Object-Relational Mapping)

```
Python Object              →    Database Table
─────────────                   ───────────────
student = Student(              INSERT INTO students
  name="Alice",                 VALUES (
  age=20,                         'Alice',
  grade="A"                       20,
)                                 'A'
                                )
```

### 2. Dependency Injection

```
@app.get("/students/")
def get_all_students(db: Session = Depends(get_db)):
                     └─────────┬─────────┘
                               │
                    Automatically injected!
                    - Creates DB connection
                    - Passes to function
                    - Closes after use
```

### 3. Pydantic Validation

```
Input Data          Validation           Result
──────────          ──────────           ──────
age: "twenty"   →   Must be integer  →   ❌ Error
age: 20         →   Check: 5-100     →   ✓ Valid
age: 150        →   Check: 5-100     →   ❌ Error
```

## 🚀 Data Flow Summary

```
                    ┌──────────┐
                    │  CLIENT  │
                    └────┬─────┘
                         │
                    HTTP Request
                         │
                         ▼
            ┌────────────────────────┐
            │  FastAPI (main.py)     │
            │  • Routes requests     │
            │  • Handles errors      │
            └────────┬───────────────┘
                     │
          ┌──────────┴──────────┐
          │                     │
          ▼                     ▼
   ┌─────────────┐      ┌─────────────┐
   │  Pydantic   │      │  Database   │
   │  Validation │      │  Operations │
   └─────────────┘      └──────┬──────┘
                               │
                               ▼
                        ┌──────────────┐
                        │  SQLAlchemy  │
                        │  ORM Layer   │
                        └──────┬───────┘
                               │
                               ▼
                        ┌──────────────┐
                        │  students.db │
                        │  SQLite File │
                        └──────────────┘
```

---

**This architecture ensures:**
- ✅ Clean separation of concerns
- ✅ Easy to test and maintain
- ✅ Type safety with Pydantic
- ✅ Automatic API documentation
- ✅ Database abstraction with ORM
